services:
  gallerai-rabbitmq:
    environment:
      - RABBITMQ_DEFAULT_USER=gallerai
      - RABBITMQ_DEFAULT_PASS=gallerai_dev_password_2024
  gallerai-db:
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=Password123!
      - POSTGRES_DB=postgres